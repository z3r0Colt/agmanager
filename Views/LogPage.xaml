<UserControl x:Class="AgApp.Views.LogPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Transparent"
             Style="{StaticResource PageTransition}"
             Loaded="OnLoaded"
             Unloaded="OnUnloaded">

    <Grid Margin="28,24,28,28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Application Log" Style="{StaticResource PageTitle}" Margin="0,0,0,16"/>

        <!-- Toolbar -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,12">
            <Button Style="{StaticResource SecondaryButton}" Margin="0,0,8,0"
                    Command="{Binding RefreshCommand}">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="&#xE72C;" FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets"
                               FontSize="13" Margin="0,0,6,0" IsHitTestVisible="False"/>
                    <TextBlock Text="Refresh" IsHitTestVisible="False"/>
                </StackPanel>
            </Button>
            <Button Style="{StaticResource SecondaryButton}" Margin="0,0,8,0"
                    Command="{Binding ClearLogCommand}">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="&#xE74D;" FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets"
                               FontSize="13" Margin="0,0,6,0" IsHitTestVisible="False"/>
                    <TextBlock Text="Clear" IsHitTestVisible="False"/>
                </StackPanel>
            </Button>
            <Button Style="{StaticResource SecondaryButton}" Margin="0,0,16,0"
                    Command="{Binding OpenLogFolderCommand}">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="&#xE838;" FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets"
                               FontSize="13" Margin="0,0,6,0" IsHitTestVisible="False"/>
                    <TextBlock Text="Open Folder" IsHitTestVisible="False"/>
                </StackPanel>
            </Button>
            <CheckBox Content="Auto-refresh (3s)" IsChecked="{Binding AutoRefresh}"
                      VerticalAlignment="Center"/>
        </StackPanel>

        <!-- Log content -->
        <Border Grid.Row="2" CornerRadius="10" Padding="1">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                    <GradientStop Color="#28ffffff" Offset="0"/>
                    <GradientStop Color="#06ffffff" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
            <Border Background="{StaticResource CardBrush}" CornerRadius="9">
                <ScrollViewer x:Name="LogScroll" VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Auto" Padding="16">
                    <TextBox Text="{Binding LogText, Mode=OneWay}"
                             IsReadOnly="True" TextWrapping="NoWrap"
                             Background="Transparent" BorderThickness="0"
                             Foreground="{StaticResource TextBrush}"
                             FontFamily="Cascadia Mono, Consolas, Courier New"
                             FontSize="11.5" VerticalAlignment="Top"/>
                </ScrollViewer>
            </Border>
        </Border>
    </Grid>
</UserControl>
